{
  "name": "wheel-of-fortune",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run copy-readme && tsc && vite build",
    "preview": "vite preview",
    "format": "prettier -luw src",
    "copy-readme": "mkdir -p public/tor && cp ./README.md ./public/tor/index.md"
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "typescript": "^5.2.2",
    "vite": "^5.3.4",
    "vite-plugin-javascript-obfuscator": "^3.1.0",
    "vite-tsconfig-paths": "^4.3.2"
  },
  "dependencies": {
    "clsx": "^2.1.1",
    "modern-normalize": "^2.0.0"
  }
}
